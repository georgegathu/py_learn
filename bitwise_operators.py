"""
Works on bits (binary numbers)
They include:
    & = bitwise AND
    | = bitwise OR
    ^ = XOR
    ~ = NOT (COMPLIMENT)
    << = LEFT SHIFT
    >> = RIGHT SHIFT
"""
x = 4
y = 5

print(x & y) # Ans: 4